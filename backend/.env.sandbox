# PayFast Sandbox Configuration (used by backend/app/payfast/*)
PAYFAST_MODE=sandbox
PAYFAST_MERCHANT_ID=10000100          # TODO: replace with your sandbox merchant_id
PAYFAST_MERCHANT_KEY=46f0cd694581a    # TODO: replace with your sandbox merchant_key if different
PAYFAST_PASSPHRASE=your_sandbox_passphrase  # Optional: only if passphrase set on account

# Callback / redirect endpoints (must be publicly reachable)
PAYFAST_RETURN_URL=https://www.cape-control.com/payfast/return
PAYFAST_CANCEL_URL=https://www.cape-control.com/payfast/cancel
PAYFAST_NOTIFY_URL=https://www.cape-control.com/api/payfast/itn

# Notes:
# - PROCESS URL (sandbox.payfast.co.za) is derived automatically; no need to set.
# - Ensure these match the routes implemented in the frontend (PayFastReturn / PayFastCancel) and backend ITN.
