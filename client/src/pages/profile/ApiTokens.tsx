const ApiTokens = () => (
  <section>
    <h1>API Tokens</h1>
    <p>Placeholder API tokens page. Render personal token list and revoke controls when backend is ready.</p>
  </section>
);

export default ApiTokens;
