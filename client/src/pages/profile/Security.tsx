const SecuritySettings = () => (
  <section>
    <h1>Security Settings</h1>
    <p>Placeholder security settings page. Implement MFA, password reset, and session management UI.</p>
  </section>
);

export default SecuritySettings;
