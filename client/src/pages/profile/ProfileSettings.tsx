const ProfileSettings = () => (
  <section>
    <h1>Profile Settings</h1>
    <p>Placeholder profile settings page. Replace with CapeWire-aligned layout.</p>
  </section>
);

export default ProfileSettings;
